#include <stdio.h>
#include <stdbool.h>

main (void)
{
    bool x, y;
    x = y = true;
    int a;
    float b = 2.37, c = 2.2;
    a = b;
    printf("O valor de x é %d", x);

    return 0;
}